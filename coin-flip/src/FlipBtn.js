import React from "react";
// import "./FlipBtn.css";

/** Card: displays image.
 * 
 * Props:
 * - caption: string describing the image
 * - src: string for the image link
 * - currNum: integer for what image we're on
 * - totalNum: integer for how many images are in the collection
 * 
 * State:
 * - none
 * 
 * App --> Carousel --> Card
 */

 function FlipBtn({caption, src, currNum, totalNum}) {

  return (
    <div className="FlipBtn">
    </div>
  );
}

export default FlipBtn;